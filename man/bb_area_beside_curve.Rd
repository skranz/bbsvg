% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bb_area.r
\name{bb_area_beside_curve}
\alias{bb_area_beside_curve}
\title{Add an area beside a curve}
\usage{
bb_area_beside_curve(
  bb,
  eq = NULL,
  curve.id = NULL,
  fill = "#8888ff",
  alpha = 0.3,
  stroke = "none",
  style = nlist(fill = fill, `fill-opacity` = alpha, stroke = stroke, ...),
  direction = c("above", "below", "left", "right")[1],
  level = -10,
  ...,
  id = random.string(),
  tooltip = NULL,
  xmin = bb$xrange[[1]],
  xmax = bb$xrange[[2]],
  ymin = bb$yrange[[1]],
  ymax = bb$yrange[[2]],
  xrange = c(xmin, xmax),
  yrange = c(ymin, ymax)
)
}
\arguments{
\item{bb}{A `bb` graphic object.}

\item{eq}{Optional curve equation as a character string.}

\item{curve.id}{Identifier of an existing curve. Used when `eq` is `NULL`.}

\item{fill}{Fill color.}

\item{alpha}{Fill opacity.}

\item{stroke}{Stroke color.}

\item{style}{A list of SVG style properties.}

\item{direction}{One of `"above"`, `"below"`, `"left"`, or `"right"`.}

\item{level}{Drawing level; lower values are drawn first.}

\item{...}{Additional arguments passed to [bb_curve()].}

\item{id}{A unique object identifier.}

\item{tooltip}{Optional tooltip text.}

\item{xmin, xmax, ymin, ymax}{Bounds used to construct the shading region.}

\item{xrange, yrange}{Two-element coordinate ranges.}
}
\value{
The modified `bb` object.
}
\description{
Shades the part of a plotting pane on a selected side of a curve.
}
\examples{
\dontrun{
bb_pane(xrange = c(0, 4), yrange = c(0, 4)) |>
  bb_area_beside_curve(eq = "y_ = x_", direction = "below")
}
}
